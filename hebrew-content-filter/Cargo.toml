[package]
name = "hebrew-content-filter"
version = "0.1.0"
edition = "2021"

[dependencies]
scraper = "0.20"
regex = "1.10"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
